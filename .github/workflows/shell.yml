name: CI
on:
  pull_request:
    branches:
      - main
    paths:
      - "bin/**"
      - "lib/**"
jobs:
  format:
    name: Check format
    uses: ./.github/workflows/reuse-beautysh.yml
  linter:
    name: Linting
    needs: format
    uses: ./.github/workflows/reuse-shellcheck.yml
